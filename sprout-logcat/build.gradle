apply from: '../basic.gradle'

version = '1.0.0'

packing = 'aar'

android {
    defaultConfig {
        versionCode 100
        versionName project.version
    }
}

dependencies {
    compile 'com.orhanobut:logger:1.15'
}